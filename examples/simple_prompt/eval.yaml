# yaml-language-server: $schema=../../schemas/eval-schema.yaml.json

checks:
  - id: aligned
    instructions: Is this a dad joke about what the user asked?
  - id: profanity

evals:
  - messages:
      - role: user
        content: tell me a joke about balconies
